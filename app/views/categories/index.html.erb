<h1>All Categories</h1>

<%= link_to "Add a Category", new_category_path %>

<ul>
  <% @categories.each do |category| %>
    <li>
      <%= link_to category.title, category_path(category) %>
      <%= button_to "Delete Category", method: :destroy %>
    </li>
  <% end %>
</ul>
